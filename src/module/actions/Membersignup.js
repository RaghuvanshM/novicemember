import { createAction } from "redux-act";

export const memberSignUp = createAction();
export const signupclick = createAction();
export const signupSuccess = createAction();
export const apiFailed = createAction();